<h1>New exercise</h1>


<% form_for([@parent,@exercise]) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => "contents" , :locals => { :f => f } %>

  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', polymorphic_path([@parent, @exercise]) %>
