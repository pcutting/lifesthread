<h1>New bp</h1>

<% form_for(@bp) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => "new" , :locals => {:f => f} %>

  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', bps_path %>
