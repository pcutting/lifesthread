<% form_for :chartoption, :url => { :action => :update } do |f| %>
  <fieldset>
    <legend>Chart Options</legend>
    <%= render :partial => "chart_check_box_tags" %>
    <%= submit_tag %>
  </fieldset>
<% end %> 

